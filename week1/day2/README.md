# Week 1 - Day 2

## Objective

To practice and strengthen SQL query-writing skills using MySQL by working with employee data and applying various SQL clauses, operators, and aggregate functions.

## Platform Used

* **MySQL v5.7**

## Topics Covered

* SELECT Statements
* WHERE Clause
* GROUP BY
* HAVING Clause
* ORDER BY with LIMIT
* DISTINCT Keyword
* Comparison Operators
* Logical Operators (`AND`, `OR`, `NOT`)
* IN and NOT IN
* BETWEEN Operator
* LIKE Operator

## Database Used

**MySQL v5.7**

## Table Used

### Employees Table

| Column Name | Data Type   |
| ----------- | ----------- |
| emp_id      | INT         |
| emp_name    | VARCHAR(50) |
| department  | VARCHAR(50) |
| salary      | INT         |
| city        | VARCHAR(50) |
| experience  | INT         |

## Concepts Practiced

### Basic Queries

* Display all employee records.
* Retrieve specific columns from the table.
* Filter records using conditional statements.

### Aggregate Functions

* `SUM()`
* `AVG()`
* `MAX()`
* `MIN()`
* `COUNT()`

### Filtering Data

* WHERE clause
* Comparison operators (`=`, `>`, `<`, `>=`, `<=`, `!=`)
* Logical operators (`AND`, `OR`, `NOT`)

### Advanced Filtering

* `IN` and `NOT IN`
* `BETWEEN`
* `LIKE` pattern matching

### Sorting and Limiting

* `ORDER BY`
* `LIMIT`

## Tasks Completed

* Created and populated the Employees table.
* Executed SQL queries covering multiple filtering and grouping techniques.
* Applied aggregate functions for data analysis.
* Used sorting and limiting to retrieve specific records.
* Practiced pattern matching and advanced filtering operations.

## Key Learnings

* Improved understanding of SQL query structure.
* Learned how to retrieve and filter data efficiently.
* Applied aggregate functions to analyze datasets.
* Understood grouping and filtering grouped results using `GROUP BY` and `HAVING`.
* Practiced pattern-based searches using `LIKE`.
* Enhanced problem-solving skills through real-world SQL scenarios.

## Summary

Successfully completed Day 2 SQL practice by implementing queries involving data retrieval, filtering, aggregation, grouping, sorting, and pattern matching. This session strengthened core SQL concepts and improved proficiency in writing efficient database queries.
